# Contract/decision evidence audit — 2026-09-02

Scope: current checked `[C]` and `[D]` atom declarations only. Every atom below is absent from the Evidence Registry (the registry's four rows map only to `[BE]`/`[FE]`/`[QA]` atoms). `API-CONTRACT.md` explicitly remains `PARTIAL` and its endpoint registry remains representative/illustrative, so a prose section is not treated as approved OpenAPI evidence.

`ATOM-ID | recommended status | exact artifact/missing-evidence reason`

GLB-C-01 | BLOCKED | `API-CONTRACT.md` §§3, 8 records only partial conventions and explicitly leaves JSON naming, enum wire values, and null/omit behavior undefined; required `ISSUES::API-001/002` resolutions and an approved/versioned OpenAPI artifact are absent.
GLB-C-02 | BLOCKED | `API-CONTRACT.md` §§4–5 sketches the envelope and list parameters but explicitly leaves `meta`, defaults/limits, error catalog, details nullability, and most HTTP mappings undefined; required `ISSUES::API-001/004` resolutions and approved OpenAPI evidence are absent.
GLB-C-03 | IN_PROGRESS | `API-CONTRACT.md` §11 identifies internal auth/session concerns, but cookie-vs-header transport, expiry, refresh, reset operations/TTL, and CSRF remain undefined; no approved/versioned auth contract is registered.
GLB-C-04 | IN_PROGRESS | `DECISIONS.md` ADR-014 and `API-CONTRACT.md` §3 establish JST business handling and UTC timestamp persistence, but `API-CONTRACT.md` §5 still says the date wire format is undefined; no approved OpenAPI version/approval row exists.
EP-001-C-01 | BLOCKED | `API-CONTRACT.md` §7 API-001 and §8.1 provide a representative login route/body, but `login_id` mapping, exact DTO/response, token transport, TTL, and lockout remain undefined; GLB-C-03 is not evidence-complete.
EP-002-C-01 | BLOCKED | `API-CONTRACT.md` §§5 and 7 API-002 explicitly leave item DTO, pagination metadata/defaults, filter grammar, sort allowlist, and state wire values undefined; global naming/envelope gates lack approved evidence.
EP-003-C-01 | BLOCKED | `API-CONTRACT.md` §7 API-003 and §10 mention a versioned submit operation, but the body/version wire field, response, and normalized conflict status/code are undefined; OPT decisions have no direct evidence.
EP-004-C-01 | BLOCKED | `Rakusai-System-Database-Design.md` §3.3 supplies the state machine, while `API-CONTRACT.md` §7 API-004 explicitly lacks AM/HQ dispatch representation, request body/token, and response; OPT/global contract gates are unverified.
EP-005-C-01 | BLOCKED | `Rakusai-System-Database-Design.md` §3.3 establishes rejection behavior, but `API-CONTRACT.md` §7 API-005 leaves `rejection_reason` versus `comment`, concurrency token, and response undefined; no approved OpenAPI/OPT evidence exists.
EP-006-C-01 | BLOCKED | `API-CONTRACT.md` §7 API-006 is illustrative and explicitly lacks the slot DTO and candidate-safe boundary; §5 leaves date format/range/pagination undefined, and the global contract gate is unresolved.
EP-007-C-01 | BLOCKED | `DECISIONS.md` ADR-010 and `API-CONTRACT.md` §§7/10 support row locking and a version concept, but candidate field, version wire name, response, and conflict HTTP/error contract remain undefined; OPT/SSI decisions are unverified.
EP-008-C-01 | BLOCKED | `API-CONTRACT.md` §7 API-008 provides only an illustrative token route and explicitly leaves safe fields and branch discriminators undefined; the token-purpose model and approved public DTO are absent.
EP-009-C-01 | BLOCKED | `API-CONTRACT.md` §7 API-009 explicitly says the survey DTO is not locked; `DATABASE-SCHEMA.md` keeps `preferred_dates` NOT NULL while the required pass-only sequencing decision is unresolved, so result/consume boundary evidence is absent.
EP-010-C-01 | BLOCKED | `API-CONTRACT.md` §7 API-010 explicitly leaves multipart limits/template columns unconfirmed; `DECISIONS.md` OPEN-005/006 and `Rakusai-System-Database-Design.md` §8 confirm the required import formats remain open.
EP-011-C-01 | BLOCKED | `API-CONTRACT.md` §§2/7 and `DECISIONS.md` OPEN-009 state the final RikuOp contract and webhook-vs-polling choice do not exist; auth, payload, acknowledgement/checkpoint, and retry/idempotency therefore have no sign-off artifact.
EP-012-C-01 | BLOCKED | `API-CONTRACT.md` §7 API-012 explicitly leaves exposure/auth and component response undefined; `ARCHITECTURE.md` gives only a general health intent, while AUT/STG decisions are incomplete.
WP-API-001-C-01 | BLOCKED | This package has no approved method/path/DTO in `API-CONTRACT.md`; §11 explicitly leaves reset request/consume endpoints and TTL undefined, and AUTH reset persistence/lifecycle has no approved decision evidence.
WP-API-002-C-01 | BLOCKED | `RULE.md` flow 2.2 and `DATABASE-SCHEMA.md` assignment tables are partial requirement/schema artifacts, but no create/assign operation exists in `API-CONTRACT.md`; the exactly-one-primary-SM constraint remains an explicit schema gap.
WP-API-003-C-01 | BLOCKED | `DATABASE-SCHEMA.md` defines only an import-log shape; no export or import-run operation/DTO exists in `API-CONTRACT.md`, and `DECISIONS.md` OPEN-005/006 leaves both file formats unresolved.
WP-API-004-C-01 | BLOCKED | `DATABASE-SCHEMA.md` provides requirement/version JSONB structures, but no draft/create/detail/edit/export/import operations or payload schema are approved in `API-CONTRACT.md`; import format OPEN-005 and OPT/RVO details remain unresolved.
WP-API-005-C-01 | BLOCKED | `RULE.md` flow 4 and the slot schema provide partial lifecycle/filter inputs, but `API-CONTRACT.md` has no mutation/timeline operations; `DATABASE-SCHEMA.md` explicitly flags cancel-enum and interval-overlap gaps.
WP-API-006-C-01 | BLOCKED | `DATABASE-SCHEMA.md` defines schedule fields/states, but no lifecycle/result operation or safe DTO is published in `API-CONTRACT.md`; location shape and token-purpose dependency are unresolved.
WP-API-007-C-01 | BLOCKED | `DATABASE-SCHEMA.md` §5.4 documents a direct conflict between `RULE.md` blacklist fields and the canonical schema; no `ISSUES::DB-007` resolution or candidate list/detail OpenAPI operation is evidenced.
WP-API-008-C-01 | BLOCKED | `DATABASE-SCHEMA.md` defines scheduled/sent/failed persistence only; no HQ monitoring read/filter operation or masked DTO appears in `API-CONTRACT.md`, and NTF/PII policies are not approved.
WP-API-009-C-01 | BLOCKED | `DECISIONS.md` ADR-012 gives only an anti-corruption pattern; the final RikuOp payload contract is explicitly absent in `API-CONTRACT.md` §2, so outbound mapping and any monitoring DTO cannot be approved.
WP-API-010-C-01 | BLOCKED | `Rakusai-System-Database-Design.md` allows HQ manual intake, but `DATABASE-SCHEMA.md` requires a non-null RikuOp candidate ID and calls manual registration an unresolved gap; no register/assign/reassign operation or `ISSUES::DB-006` resolution exists.
WP-API-011-C-01 | BLOCKED | No candidate-safe availability/preference method/path/DTO is present in `API-CONTRACT.md`; API-006 is explicitly internal/illustrative, and token purpose, eligible state, date range, stale semantics, and the 36-hour boundary lack an approved artifact.
WP-API-012-C-01 | BLOCKED | `Rakusai-System-Database-Design.md` describes survey then matching, while `DATABASE-SCHEMA.md` makes `preferred_dates` mandatory in the single survey response; no decision resolves staged-vs-atomic persistence or publishes operation boundaries.
WP-API-013-C-01 | BLOCKED | `DATABASE-SCHEMA.md` provides append-only approval rows and the design notes AM self-approval, but no history operation/DTO/order/pagination/scope representation exists in `API-CONTRACT.md`; RVO representation is unapproved.
WP-API-014-C-01 | BLOCKED | `DECISIONS.md` ADR-006 and `DATABASE-SCHEMA.md` define only a survey token with hash/expiry/used fields; there is no approved per-purpose lifecycle matrix for survey, result, and reminder or an OpenAPI contract.
GLB-C-05 | BLOCKED | `API-CONTRACT.md` §§4, 8 explicitly leave property presence/null/omit behavior and API enum mappings undefined; required `ISSUES::API-001/002/004` resolutions and approved OpenAPI evidence are absent.
WP-API-002-C-02 | BLOCKED | No Area/Store/assignable-manager read operation, minimal DTO, or scope is published in `API-CONTRACT.md`; the tracker itself forbids inventing a method/path and no approval artifact exists.
WP-API-014-D-01 | IN_PROGRESS | `DECISIONS.md` ADR-006 and `DATABASE-SCHEMA.md` `candidate_survey_tokens` are partial artifacts for one survey credential, but neither decides whether result/reminder reuse it nor defines purpose storage/isolation.
NTF-D-04 | IN_PROGRESS | `Rakusai-System-Database-Design.md` §3.6 contains a source event→recipient→channel→timing table, but it does not cover every tracker occurrence or carry decision version, approval/date, purpose, suppression, or occurrence identity.
P4-07-C-01 | BLOCKED | The design states only “5 days elapsed” and a reminder job; exact due boundary, late-submit cancellation/suppression, and RikuOp effects are absent, and REM-D-01…03 plus OPEN-007 are unresolved.
AUTH-D-01 | IN_PROGRESS | `API-CONTRACT.md` §11 and `ARCHITECTURE.md` §5.1 mention session/JWT generically, but transport, persistence, refresh/revocation, invalidation, and internal-account linkage remain explicitly undefined; no approved ADR for these choices exists.
AUTH-D-02 | BLOCKED | `DATABASE-SCHEMA.md` has no password-reset token model and `API-CONTRACT.md` §11 leaves reset endpoints/TTL undefined; the task also depends on unapproved GLB-C-03 and WP-API-001-C-01.
OPT-D-01 | IN_PROGRESS | `Rakusai-System-Database-Design.md` §§3.5/6.4 and `ARCHITECTURE.md` §7 identify requirement-edit and slot-booking concurrency, but do not approve the complete edit/submit/approve/reject/update/book boundary matrix.
OPT-D-02 | IN_PROGRESS | Source docs establish optimistic versions and row locking, but `API-CONTRACT.md` §10 explicitly leaves the wire field and conflict HTTP/code undefined; increment/change, ABA, and authoritative-state response rules are missing.
RVO-D-01 | IN_PROGRESS | `DECISIONS.md` ADR-007 and `DATABASE-SCHEMA.md` define current/published pointers and immutable versions, but their nullability/lifecycle for every draft/reject/resubmit/approve transition is not captured in an approved invariant matrix.
RVO-D-02 | BLOCKED | `DATABASE-SCHEMA.md` §9 explicitly leaves circular-FK migration order/deferrability unresolved; DB/application enforcement, allocation, archive/delete policy, and required `ISSUES::DB-008` resolution have no decision artifact.
SSI-D-01 | IN_PROGRESS | Slot and schedule both carry Store in `DATABASE-SCHEMA.md`, and ADR-010 ties slots to a responsible SM, but no decision states whether schedule Store is derived, denormalized, or independent or defines the three-way invariant.
SSI-D-02 | BLOCKED | Source docs describe cross-Store booking risk and manager assignments, but cross-Store reschedule/reassignment semantics are absent; the primary-manager/assignment invariant remains an explicit schema gap with no `ISSUES::DB-004` resolution.
NTF-D-01 | IN_PROGRESS | `DECISIONS.md` ADR-011 and `ARCHITECTURE.md` §8 name a persisted notification/outbox, but the atomic business-transaction→outbox→relay/BullMQ handoff and failure boundary are not fully specified or approved.
NTF-D-02 | IN_PROGRESS | `DATABASE-SCHEMA.md` supplies scheduled/sent/failed and idempotency fields, but claim/lease/reclaim, retry exhaustion/DLQ, and canonical occurrence identity are absent; the schema even has no dead-letter state.
NTF-D-03 | BLOCKED | The specified source set contains no provider contract or provider message-id/idempotency behavior, so ambiguous timeout and external dedupe semantics cannot be decided.
IMP-D-01 | BLOCKED | `ARCHITECTURE.md` shows an import queue but no durable object-storage abstraction or upload→persist→queue acknowledgement lifecycle; staging/storage topology is unresolved.
IMP-D-02 | BLOCKED | `DATABASE-SCHEMA.md` `master_data_import_logs` lacks durable reference, checksum, run state, file type/size/access/encryption/retention/quarantine fields; OPEN-005/006 leave file contracts unresolved.
IMP-D-03 | BLOCKED | `DECISIONS.md` OPEN-005/006 explicitly leaves parser/file formats open, and no source specifies file/run/queue atomicity or row semantics; required issue resolutions are absent.
PII-D-01 | IN_PROGRESS | The schema identifies relevant JSONB columns and some actors, but no consolidated data inventory/classification or actor-access matrix exists for audit, sync, survey, notification, location, and import errors.
PII-D-02 | BLOCKED | No data-owner/security/legal approval, allowlist, masking/encryption matrix, retention/erasure/legal-hold, or backup policy exists in the specified sources.
PII-D-03 | IN_PROGRESS | Source security rules prohibit plaintext passwords/secrets and token plaintext, but no approved sanitized projections, log-field allowlist, or JSONB schema-versioning policy exists.
WRK-D-01 | IN_PROGRESS | `ARCHITECTURE.md` §6 lists five queues/jobs, but does not decide same-process versus separate workers or assign complete queue-to-worker ownership/deployment units.
WRK-D-02 | IN_PROGRESS | The deployment and queue artifacts are partial, but concurrency, provider rate limits, autoscaling, isolation, scheduler HA, job retention, and deploy-compatibility rules are missing.
WRK-D-03 | IN_PROGRESS | `ARCHITECTURE.md` covers ConfigService, DB/Redis/Secrets and generic health, but worker probes, graceful drain/shutdown, and stalled-job recovery are not specified or approved.
AUD-D-01 | IN_PROGRESS | `DECISIONS.md` ADR-005/017 and `ARCHITECTURE.md` show mutating-request audit writes, but transaction atomicity and failure policy, including failed/security events versus success audit, are undefined.
AUD-D-02 | IN_PROGRESS | The sources mention a global interceptor, yet do not decide application/unit-of-work ownership, retry correlation, system actor behavior, or multi-entity action representation.
AUD-D-03 | BLOCKED | `DATABASE-SCHEMA.md` exposes before/after JSONB without a redaction or retention policy; completion depends on the absent data-owner/security/legal policy in PII-D-02.
REM-D-01 | IN_PROGRESS | Source docs list reminder events and a generic notification idempotency key, but no approved occurrence identity joins requirement/version/state-entry/recipient/channel/template/business date.
REM-D-02 | BLOCKED | `Rakusai-System-Database-Design.md` §3.6 explicitly says caps, send windows, and holiday handling are pending; `DECISIONS.md` OPEN-007 remains unresolved, and missed-run/catch-up is unspecified.
REM-D-03 | IN_PROGRESS | `ARCHITECTURE.md` lists reminder jobs/triggers, but per-entity-vs-scanner choice, recipient snapshot policy, authoritative stop/resume, and state re-entry behavior are absent.
AUT-D-01 | IN_PROGRESS | `API-CONTRACT.md` and `ARCHITECTURE.md` name internal, candidate-public, RikuOp, and health classes, but no exhaustive approved route inventory/classification artifact exists.
AUT-D-02 | IN_PROGRESS | Uniform guards and public rate limiting are source-backed, but default-deny metadata plus per-class CORS/CSRF/rate/cache policy are missing; cookie/CSRF choice is explicitly unresolved in `API-CONTRACT.md` §11.
AUT-D-03 | BLOCKED | RikuOp authentication/signature/replay is explicitly undefined and depends on the absent final external contract; health exposure/minimal response is also undefined in `API-CONTRACT.md` API-012.
ERD-D-01 | IN_PROGRESS | `DATABASE-SCHEMA.md` contains table definitions and Mermaid relations, but not a min..max logical-vs-physical matrix; nullable actor/store/submitter semantics and FK delete behavior are incomplete.
ERD-D-02 | BLOCKED | Polymorphic references are documented, but `DATABASE-SCHEMA.md` §9 explicitly leaves primary-SM, RVO, SSI, and related cardinality/integrity gaps unresolved; no approved decision matrix exists.
STG-D-01 | BLOCKED | `Rakusai-System-Database-Design.md` and `ARCHITECTURE.md` retain “Docker Compose or single ECS” for staging; owner, criteria, budget, account, and network inputs are absent.
STG-D-02 | BLOCKED | Source docs retain alternatives for staging compute/DB and production Web (`Vercel` or `CloudFront+S3`); no selected compute/DB/Redis/Web/storage topology or parity-deviation approval exists.
STG-D-03 | BLOCKED | No source decision fixes worker placement, TLS/network, migration runner, logs/metrics, or backup operations for staging; these cannot be inferred from the generic topology table.
SRCCTL-D-01 | IN_PROGRESS | `Rakusai-System-Database-Design.md` §3.3 is canonical and `API-CONTRACT.md` follows its rejected state, but `RULE.md` still presents the conflicting HQ return flow and no explicit superseding ADR/issue synchronization evidence exists.
SRCCTL-D-02 | IN_PROGRESS | `Rakusai-System-Database-Design.md` §3.4 defines adjustment only after no matching slot, but the conflicting `RULE.md` preference flow is not marked superseded and no dedicated accepted source-control decision/evidence row exists.
SRCCTL-D-03 | BLOCKED | `DATABASE-SCHEMA.md` keeps `preferred_dates` NOT NULL while the contract requires pass-only preference sequencing; no accepted decision or OpenAPI/schema resolution exists, and WP-API-012-C-01 is unverified.
NS-DOC-01 | IN_PROGRESS | The tracker states an EP↔API mapping, but `API-CONTRACT.md` still declares `API-001…012`; there is no approved/date/versioned mapping artifact or direct evidence row demonstrating canonical namespace adoption.
NS-DOC-02 | BLOCKED | Current tracker dependencies still use `ISSUES::API-001…009`, and none of the allowed source artifacts provides an approved `ISS-API-*` migration/reference-resolution report.
NS-DOC-03 | BLOCKED | No allowed source artifact records approved new issues/resolutions for manual candidate, safe availability, approval history, token lifecycle, or sequencing/schema gaps; the underlying gaps remain explicit in `API-CONTRACT.md`/`DATABASE-SCHEMA.md`.
NS-DOC-04 | BLOCKED | `ARCHITECTURE.md` still keeps staging/Web alternatives, `DECISIONS.md` keeps RikuOp mechanism OPEN-009, and the schema retains derived-source ambiguities; the requested ADR corrections are not complete.
SRC-ROUTE-C-01 | BLOCKED | The specified contract sources contain only a representative/illustrative endpoint registry, not an approved source-route manifest or surplus-route disposition; P0-04 and AUT-D-01 therefore lack valid evidence.

## Totals

- Audited checked `[C]`/`[D]` DONE atoms: 77
- Direct Evidence Registry rows: 0
- Recommend `IN_PROGRESS`: 26
- Recommend `BLOCKED`: 51
